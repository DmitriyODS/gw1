# Скопируйте в .env и заполните:
#   cp .env.example .env

# PostgreSQL
POSTGRES_DB=tasktime
POSTGRES_USER=tasktime
POSTGRES_PASSWORD=changeme

# JWT — случайная строка, мин. 32 символа
JWT_SECRET=change-me-to-a-random-secret-string
